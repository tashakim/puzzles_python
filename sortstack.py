class SortStack:
	"""Purpose: Sorts a stack such that the smallest items
	are on the top, 
	"""
	def __init__(self, cap):
		self._data = [None]*cap
		self._capacity = cap
		self._top = 0
		return

	def push(self, item):

		return
	def pop(self):
		return

	def printStack(self):
		return

if __name__ == "__main__":
	s= SortStack()