def blocks(n):
	"""Purpose: Returns the max no. of possible staircases, given n, number of blocks.
	"""



if __name__ == "__main__":
	assert(blocks(3) == 1), "Wrong"
	assert(blocks(4) == 1), "Wrong"
	assert(blocks(5) == 2), "Wrong"