def eulerTour(node):
	"""Purpose: Python implementation of euler tour traversal.
	"""
	visitLeft(node)
	if(node hasLeft()):
		eulerTour(node.left())

	visit
	if(node hasRight()):
		eulerTour(node.right())

	visitRight(node)